apiVersion: apps/v1
kind: Deployment
metadata:
  name: establishment-service
  namespace: default
  labels:
    app: establishment-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: establishment-service
  template:
    metadata:
      labels:
        app: establishment-service
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: establishment-service
          image: ghcr.io/GITHUB_OWNER/vibester-establishment-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3002
          envFrom:
            - secretRef:
                name: establishment-service-secret
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 20
            periodSeconds: 30
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: establishment-db
  namespace: default
  labels:
    app: establishment-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: establishment-db
  template:
    metadata:
      labels:
        app: establishment-db
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: establishment-service-secret
          volumeMounts:
            - name: establishment-db-data
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
      volumes:
        - name: establishment-db-data
          persistentVolumeClaim:
            claimName: establishment-db-pvc
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: establishment-db-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
