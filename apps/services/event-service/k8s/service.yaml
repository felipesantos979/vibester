apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: default
  labels:
    app: event-service
spec:
  type: ClusterIP
  selector:
    app: event-service
  ports:
    - protocol: TCP
      port: 3334
      targetPort: 3334
